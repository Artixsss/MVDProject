# MVD Frontend — Система учёта обращений граждан в МВД

Фронтенд приложение на Flutter Web для работы с обращениями граждан в МВД России.

## 🎯 Основные возможности

### Для граждан (публичный доступ)
- ✅ **Подача обращений** — форма для подачи жалоб/заявлений без авторизации
- ✅ **Проверка статуса** — отслеживание состояния обращения по номеру
- ✅ **Автоопределение района** — геокодирование адреса через Nominatim
- ✅ **Интерактивная карта** — просмотр местоположения инцидента

### Для сотрудников МВД (требуется авторизация)
- ✅ **Дэшборд** — KPI-панель, интерактивная карта инцидентов с кликабельными маркерами
- ✅ **Список обращений** — фильтры по категории/району/статусу, поиск, быстрые действия
- ✅ **Детали обращения** — полная информация, AI-анализ, действия (смена статуса, назначение исполнителя)
- ✅ **Создание обращений** — форма с автозаполнением и геокодированием
- ✅ **Аналитика** — статистика по районам, категориям, AI-покрытие
- ✅ **Администрирование** — управление справочниками (категории, типы, сотрудники)

### Интеграция с бэкендом
- ✅ Все API-эндпоинты бэкенда реализованы
- ✅ AI-анализ обращений (категория, приоритет, тональность, резюме, рекомендации)
- ✅ Геокодирование адресов
- ✅ CRUD операции для всех сущностей

## 🛠️ Технологии

- **Flutter** ≥ 3.8 (Web)
- **Dart** SDK ^3.8.1
- **Material 3** — современный UI/UX дизайн
- **go_router** ^14.0.0 — навигация с deep linking
- **flutter_map** ^6.0.0 — интерактивные карты (OpenStreetMap)
- **syncfusion_flutter_charts** ^31.1.19 — графики и диаграммы
- **shared_preferences** ^2.3.0 — хранение сессии
- **http** ^1.2.2 — HTTP-клиент

## 📁 Структура проекта

```
lib/
├── main.dart                    # Точка входа
├── app_router.dart              # Конфигурация маршрутов
├── services/
│   └── api_service.dart         # API-клиент (все эндпоинты бэкенда)
├── models/
│   ├── citizen_request.dart     # Модели обращений
│   └── user_session.dart        # Модель сессии пользователя
├── screens/
│   ├── auth_screen.dart         # Авторизация + ссылки для граждан
│   ├── citizen_complaint_screen.dart    # Подача обращений гражданами
│   ├── check_status_screen.dart         # Проверка статуса по номеру
│   ├── dashboard_screen.dart            # Главная панель с картой
│   ├── request_list_screen.dart         # Список обращений с фильтрами
│   ├── request_detail_screen.dart       # Детальная информация
│   ├── request_new_screen.dart          # Создание обращения сотрудником
│   ├── analytics_screen.dart            # Аналитика
│   └── admin_screen.dart                # Справочники
├── widgets/
│   ├── home_action.dart         # Виджет действия "Домой"
│   └── user_profile_action.dart # Виджет профиля пользователя
└── utils/
    └── constants.dart           # Константы (baseUrl и т.д.)
```

## 🚀 Запуск приложения

### Предварительные требования
1. Flutter SDK ≥ 3.8
2. Dart SDK ^3.8.1
3. Запущенный бэкенд на `http://localhost:5029`

### Установка зависимостей
```bash
flutter pub get
```

### Запуск в режиме разработки
```bash
# Для Chrome
flutter run -d chrome

# Для Edge
flutter run -d edge

# Альтернатива: использовать скрипты
start.bat      # Windows (CMD)
start.ps1      # Windows (PowerShell)
```

### Сборка для продакшена
```bash
flutter build web --release
```

Собранные файлы будут в `build/web/`

## 🌐 Маршруты приложения

### Публичные (без авторизации)
- `/` — Страница авторизации + ссылки для граждан
- `/complaint` — Подать обращение (для граждан)
- `/check-status` — Проверить статус обращения (для граждан)

### Защищённые (требуется авторизация)
- `/dashboard` — Главная панель сотрудника
- `/requests` — Список обращений
- `/requests/new` — Создать новое обращение
- `/requests/:id` — Детали обращения
- `/analytics` — Аналитика
- `/admin` — Администрирование (только Admin/Manager)

## 🔐 Авторизация

### Тестовые данные
- **Логин:** `kozlova`
- **Пароль:** `operator123`
- **Роль:** Operator

> ⚠️ **Примечание:** В текущей версии JWT не реализован. Авторизация временно сохраняется в `SharedPreferences`.

## 🎨 Дизайн-система

### Цветовая палитра
- **Основной:** `#0D47A1` (темно-синий МВД)
- **Акцент:** `#1565C0`
- **Фон:** `#FFFFFF` / `#F5F7FA`
- **Текст:** `#212121` / `#424242`

### Приоритеты (AI)
- 🔴 **Высокий** — красный
- 🟠 **Средний** — оранжевый
- 🟢 **Низкий** — зелёный

### Статусы обращений
- 🔵 **Новый** — синий
- 🟠 **В работе** — оранжевый
- 🟢 **Закрыт** — зелёный

## 📡 API Интеграция

Все эндпоинты бэкенда реализованы в `ApiService`:

### Auth
- `POST /api/auth/employee-login` — Вход сотрудника

### Citizens
- `GET /api/citizens` — Список граждан
- `POST /api/citizens` — Создать гражданина
- `PUT /api/citizens/:id` — Обновить гражданина
- `DELETE /api/citizens/:id` — Удалить гражданина

### CitizenRequests
- `GET /api/citizenrequests` — Список обращений (с фильтрами)
- `GET /api/citizenrequests/:id` — Получить обращение
- `GET /api/citizenrequests/check/:requestNumber` — Проверить статус (для граждан)
- `GET /api/citizenrequests/by-number/:requestNumber` — Получить по номеру
- `POST /api/citizenrequests` — Создать обращение
- `PUT /api/citizenrequests/:id` — Обновить обращение
- `DELETE /api/citizenrequests/:id` — Удалить обращение
- `PATCH /api/citizenrequests/:id/status` — Изменить статус
- `PATCH /api/citizenrequests/:id/assign` — Назначить исполнителя
- `PATCH /api/citizenrequests/:id/reclassify` — Пересчитать AI-анализ
- `PATCH /api/citizenrequests/:id/correct-category` — Скорректировать категорию AI
- `POST /api/citizenrequests/analyze` — Анализ текста AI (без сохранения)
- `POST /api/citizenrequests/generate-response` — Генерация официального ответа

### Analytics
- `GET /api/analytics/districts` — Статистика по районам
- `GET /api/analytics/categories` — Статистика по категориям
- `GET /api/analytics/ai` — Статистика AI (покрытие, коррекции)

### Справочники
- `GET /api/categories` — Категории обращений
- `GET /api/requesttypes` — Типы обращений
- `GET /api/requeststatuses` — Статусы обращений
- `GET /api/employees` — Сотрудники
- `GET /api/districts` — Районы

## 🗺️ Карта инцидентов

### Возможности
- **Интерактивные маркеры** — кликабельные пины на карте
- **Цветовая индикация** — по приоритету AI (красный/оранжевый/зелёный)
- **Информационные popup** — детали при клике на маркер
- **Управление** — зум +/-, центрирование
- **Легенда** — пояснение цветов

### Технологии
- **OpenStreetMap** — бесплатные тайлы карты
- **flutter_map** — рендеринг карты
- **latlong2** — работа с координатами

## 📊 Аналитика

### Доступные виды
- **По районам** — количество обращений и средний приоритет
- **По категориям** — распределение обращений
- **AI-статистика** — покрытие анализа, процент коррекций

### Визуализация
- **Bar charts** — гистограммы
- **Pie charts** — круговые диаграммы
- **Таблицы** — детальные данные

## 🧪 Тестирование

```bash
# Запуск тестов
flutter test

# Анализ кода
flutter analyze
```

## 🔧 Конфигурация

### Изменение базового URL
Откройте `lib/utils/constants.dart` и измените:
```dart
const String baseUrl = 'http://localhost:5029';
```

### Отключение тестовых данных
В `lib/screens/auth_screen.dart` удалите предзаполнение логина/пароля:
```dart
final _usernameController = TextEditingController(); // было: text: 'kozlova'
final _passwordController = TextEditingController(); // было: text: 'operator123'
```

## 📝 TODO / Планы развития

- [ ] Реализация JWT-авторизации
- [ ] Кластеризация маркеров на карте
- [ ] Экспорт отчётов (PDF, Excel)
- [ ] Push-уведомления
- [ ] Темная тема
- [ ] Мобильная версия (Android/iOS)
- [ ] Офлайн-режим (кэширование)
- [ ] WebSocket для real-time обновлений

## 📄 Лицензия

Проект разработан в учебных целях для практики МВД.

## 👥 Команда

Разработано студентами в рамках производственной практики.

## 📞 Поддержка

По вопросам обращайтесь к руководителю практики.

---

**Версия:** 1.0.0  
**Последнее обновление:** 6 ноября 2025
