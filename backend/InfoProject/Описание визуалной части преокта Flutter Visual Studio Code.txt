🤖 Контекст проекта для ИИ-ассистентов
Этот блок обязателен для понимания проекта любой нейросетью. Не удалять. 
📌 Название проекта
MVD Practice — Система учёта обращений граждан в МВД
📁 Структура локального репозитория
1
2
3
C:\Users\Artixs\source\repos\
├── MvdBackend\          ← Бэкенд на C# (.NET 8, ASP.NET Core Web API)
└── mvd_frontend\        ← Фронтенд на Flutter Web (Dart)
🔧 Стек технологий
•	Бэкенд:
•	Язык: C#
•	Фреймворк: ASP.NET Core 8
•	ORM: Entity Framework Core + Npgsql
•	База данных: PostgreSQL 16 + PostGIS
•	Аутентификация: временно отсутствует (JWT не реализован)
•	Интеграции: Google Gemini API, Nominatim (OpenStreetMap)
•	GitHub: https://github.com/Attys192/MvdBackend
•	Фронтенд:
•	Язык: Dart
•	Фреймворк: Flutter (Web)
•	Версия Flutter: ≥ 3.22 (Material 3)
•	HTTP-клиент: пакет http: ^0.15.0
•	Навигация: go_router
•	Карта: flutter_map + latlong2
•	Графики: syncfusion_flutter_charts
•	Хранение сессии: shared_preferences
🌐 Базовый URL API
1
http://localhost:5029
Сервер запускается командой dotnet run в папке MvdBackend.
Все эндпоинты доступны без токена авторизации (авторизация не реализована). 
🔐 Учётные данные для теста
•	Логин: kozlova
•	Пароль: operator123
•	Роль: Operator
•	ФИО: Козлов Александр Сергеевич
🗃️ Основные сущности БД
•	CitizenRequests — обращения граждан
•	Citizens — граждане
•	Employees — сотрудники МВД
•	Categories — категории обращений
•	RequestTypes — типы обращений
•	RequestStatuses — статусы
•	Districts — районы
•	AuditLogs — лог изменений
🎨 Дизайн-система
•	Цветовая палитра:
•	Основной: #0D47A1 (темно-синий МВД)
•	Акцент: #1565C0
•	Фон: #FFFFFF / #F5F7FA
•	Стиль: Material 3 (Material You)
•	Типографика: Roboto или Google Sans
•	Анимации: плавные переходы, skeleton-лоадеры, micro-interactions
⚠️ Важно для генерации кода
1.	Не использовать JWT — авторизация отсутствует.
2.	Все запросы — к http://localhost:5029 (без токена).
3.	Координаты: latitude (Y), longitude (X) — числа типа double?.
4.	AI-поля в CitizenRequestDto: aiCategory, aiPriority, aiSentiment, aiSummary, aiSuggestedAction.
5.	Номер обращения — строка вида "ABCD123456".
6.	Дата/время — в формате ISO 8601: "2025-10-25T08:30:00Z".
7.	PATCH-запросы — отправляют только тело, без id в URL (он уже в пути).
8.	POST /api/citizenrequests — возвращает 201 Created с полным DTO, включая requestNumber.


Создание визуальной части производсвтенной практики МВД

Описание визуала и требований к визуальной части проекта
## 🎨 **Проект: MVD Dashboard — Визуальная часть (Flutter Web)**  
**Цель**: Создать интуитивный, функциональный и визуально строгий интерфейс для работы с обращениями граждан в системе МВД.

---

## 🎯 **1. Общие принципы дизайна**

| Критерий | Реализация |
|--------|-----------|
| **Цветовая палитра** | Основной: `#0D47A1` (темно-синий МВД), акцент: `#1565C0`, фон: `#FFFFFF` / `#F5F7FA`, текст: `#212121` / `#424242` |
| **Стиль** | **Material 3 (Material You)** — адаптивные компоненты, elevation, shape tokens, динамические цвета |
| **Типографика** | `Roboto` или `Google Sans` — заголовки bold, тело — regular, монопространство для номеров обращений |
| **Доступность** | Контраст ≥ 4.5:1, поддержка screen reader, крупные тап-зоны |
| **Анимации** | Плавные переходы (fade, slide), skeleton-лоадеры, micro-interactions (наведение, нажатие) |

---

## 🖥️ **2. Структура приложения (навигация)**

Используем **`GoRouter`** для навигации с поддержкой deep linking.

```
/ → Авторизация  
/dashboard → Главная (KPI + карта)  
/requests → Список обращений  
/requests/:id → Детали обращения  
/requests/new → Создание обращения  
/analytics → Аналитика  
/admin → Справочники (категории, типы, сотрудники)  
/profile → Профиль пользователя
```

---

## 📱 **3. Описание экранов**

### 🔐 **Экран 1: Авторизация (`/`)**

**Компоненты**:
- Логотип МВД (или абстрактный герб)
- Поля: `Логин`, `Пароль` (с иконками)
- Кнопка: **Войти**
- После входа — сохраняем в `SharedPreferences`:
  ```dart
  { "id": 1, "username": "kozlova", "role": "Operator", "fullName": "Козлова А.В." }
  ```

**Технологии**: `TextField`, `ElevatedButton`, `FutureBuilder` для запроса.

---

### 📊 **Экран 2: Дэшборд (`/dashboard`)**

**Секции**:
1. **KPI-панель** (3 карточки):
   - Всего обращений: `124`
   - Проанализировано ИИ: `92%`
   - Коррекций ИИ: `18%`

2. **Карта инцидентов** (основной элемент):
   - Библиотека: **`flutter_map`** + **`OpenStreetMap`** (без ключа)
   - Маркеры:
     - 🔴 Красный — `AiPriority = "Высокий"`
     - 🟠 Оранжевый — `"Средний"`
     - 🟢 Зелёный — `"Низкий"`
   - Кластеризация при зуме
   - При клике — карточка: номер, категория, время, статус

3. **График категорий**:
   - Библиотека: **`syncfusion_flutter_charts`**
   - Горизонтальный bar chart: категории → количество

**Фильтры сверху**: район, категория, статус, дата.

---

### 📋 **Экран 3: Список обращений (`/requests`)**

**Вид**: адаптивная сетка — карточки на мобильном, таблица на десктопе.

**Каждая карточка содержит**:
- Номер обращения: `ABCD123456` (моноширинный шрифт)
- Статус (с цветом: новый — синий, в работе — оранжевый, закрыт — серый)
- Категория (иконка + текст)
- Район
- Время инцидента
- Кнопки быстрых действий:
  - 🔄 Изменить статус (выпадающий список)
  - 👤 Назначить исполнителя

**Фильтрация**: через `SliverAppBar` с `SearchBar` и `FilterChip`.

---

### 📄 **Экран 4: Детали обращения (`/requests/:id`)**

**Секции**:
1. **Основная информация**:
   - Гражданин, тип, категория, статус, номер, дата
2. **Геолокация**:
   - Мини-карта (`flutter_map`, 200x200) с пином
   - Адреса: инцидент / гражданин
3. **AI-анализ** (если есть):
   - Категория (редактируемая через кнопку ✏️)
   - Тональность: `Негативный` (цвет: красный)
   - Приоритет: `Высокий`
   - Резюме: краткий текст
   - Рекомендуемое действие
4. **Действия**:
   - Кнопки: **Назначить**, **Изменить статус**, **Пересчитать ИИ**, **Скорректировать категорию**

**История изменений**: пока не реализована на бэке → можно скрыть или заглушка.

---

### ➕ **Экран 5: Создание обращения (`/requests/new`)**

**Форма**:
- Выбор гражданина (autocomplete из `/api/citizens`)
- Тип обращения (dropdown из `/api/requesttypes`)
- Категория (dropdown из `/api/categories`)
- Статус (по умолчанию "Новый")
- Принявший сотрудник (автоподстановка текущего)
- Исполнитель (dropdown из `/api/employees`)
- Дата/время инцидента (DateTime picker)
- Адрес инцидента (TextField → после ввода показывает превью координат)
- Адрес гражданина
- Описание (большое поле)

**После отправки**:
- Показывает toast: «Обращение создано. Номер: ABCD123456»
- Переход на детали

---

### 📈 **Экран 6: Аналитика (`/analytics`)**

**Вкладки**:
1. **По районам**:
   - Heatmap на карте + bar chart
2. **По категориям**:
   - Pie chart + таблица
3. **ИИ-статистика**:
   - Покрытие (%), коррекции (%), распределение приоритетов

**Библиотеки**: `syncfusion_flutter_charts`, `flutter_map` для heatmap overlay.

---

### ⚙️ **Экран 7: Справочники (`/admin`)**

Только для ролей **Manager/Admin** (скрывать для Operator).

- CRUD-интерфейсы для:
  - Категорий
  - Типов обращений
  - Статусов
  - Сотрудников

**Компоненты**: `DataTable`, `FloatingActionButton`, диалоговые окна редактирования.

---

## 🧩 **4. Используемые технологии (Flutter Web)**

| Функция | Пакет |
|--------|------|
| HTTP-запросы | `http: ^0.15.0` |
| Навигация | `go_router: ^14.0.0` |
| Карта | `flutter_map: ^6.0.0` + `latlong2: ^0.9.0` |
| Графики | `syncfusion_flutter_charts: ^23.2.7` |
| Лоадеры | `shimmer: ^3.0.0` |
| Хранение сессии | `shared_preferences: ^2.3.0` |
| Валидация форм | `form_validator: ^2.0.0` |
| UI-компоненты | `Material 3` (встроено в Flutter 3.7+) |

> 💡 Все пакеты поддерживают **Flutter Web**.

---

## 🔐 **5. Авторизация (временное решение)**

Поскольку JWT не реализован:
- После логина сохраняем данные пользователя в `SharedPreferences`.
- При каждом запросе — **ничего не отправляем** (бэкенд не требует авторизации).
- На фронтенде просто проверяем: если нет данных в `SharedPreferences` → редирект на `/`.

> ⚠️ В продакшене нужно будет добавить `Authorization: Bearer <token>`.

---

## 🧪 **6. Пример запроса (Dart)**

```dart
Future<CitizenRequestDto> createRequest(CreateRequestDto dto) async {
  final response = await http.post(
    Uri.parse('http://localhost:5029/api/citizenrequests'),
    headers: {'Content-Type': 'application/json'},
    body: jsonEncode(dto.toJson()),
  );
  if (response.statusCode == 201) {
    return CitizenRequestDto.fromJson(jsonDecode(response.body));
  } else {
    throw Exception('Ошибка создания обращения');
  }
}
```

---

## 📁 **7. Структура проекта (рекомендуемая)**

```
lib/
├── main.dart
├── app_router.dart
├── services/
│   ├── api_service.dart
│   └── auth_service.dart
├── models/
│   ├── citizen_request.dart
│   ├── user.dart
│   └── ...
├── screens/
│   ├── auth_screen.dart
│   ├── dashboard_screen.dart
│   ├── request_list_screen.dart
│   ├── request_detail_screen.dart
│   └── ...
├── widgets/
│   ├── kpi_card.dart
│   ├── incident_marker.dart
│   └── ...
└── utils/
    └── constants.dart  // baseUrl, цвета
```

---



