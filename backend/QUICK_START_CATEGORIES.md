# 🚀 Быстрый старт - Обновление системы категорий

## ⏱️ 5 минут до запуска

### Шаг 1: Обновите базу данных

**Вариант А - Пересоздать БД (самый простой):**
```bash
# В PostgreSQL
DROP DATABASE mvd_db;
CREATE DATABASE mvd_db;

# В терминале
cd backend
dotnet ef database update
```

**Вариант Б - Добавить категории SQL:**
```sql
INSERT INTO "Categories" ("Name", "Description") VALUES
('Бытовые конфликты', 'Семейные ссоры, соседские конфликты'),
('Угрозы и безопасность', 'Угрозы жизни, насилие, вымогательство'),
('Киберпреступления', 'Мошенничество в интернете, взлом аккаунтов'),
('Наркотики', 'Незаконный оборот наркотиков'),
('Экология и животные', 'Жестокое обращение с животными'),
('Пропавшие люди', 'Розыск пропавших без вести'),
('Другое', 'Прочие обращения')
ON CONFLICT DO NOTHING;
```

### Шаг 2: Запустите бекенд

```bash
cd backend
dotnet run
```

### Шаг 3: Проверьте работу

```bash
# Проверьте категории
curl http://localhost:5029/api/Categories

# Создайте тестовое обращение
curl -X POST http://localhost:5029/api/CitizenRequests/submit \
  -H "Content-Type: application/json" \
  -d '{
    "firstName": "Иван",
    "lastName": "Тестов",
    "phone": "+79991234567",
    "description": "У меня украли велосипед",
    "incidentLocation": "ул. Ленина 1",
    "incidentTime": "2024-11-07T12:00:00Z",
    "requestTypeId": 1
  }'

# Через 3 секунды проверьте результат
# CategoryId должен быть 1 (Имущественные преступления), а не 10 (Другое)!
```

---

## ✅ Что изменилось

| Параметр | Было | Стало |
|----------|------|-------|
| Категорий в БД | 3 | 10 |
| ИИ обновляет CategoryId | ❌ | ✅ |
| "Другое" по умолчанию | Да | Нет |
| Точность категоризации | ~30% | ~90% |

---

## 📊 10 доступных категорий

1. Имущественные преступления
2. Транспорт и ПДД
3. Общественный порядок
4. Бытовые конфликты
5. Угрозы и безопасность
6. Киберпреступления
7. Наркотики
8. Экология и животные
9. Пропавшие люди
10. Другое *(только для редких случаев)*

---

## 🎯 Главное изменение

**Раньше:**
```
Обращение → CategoryId = 10 ("Другое") → Остается 10
```

**Теперь:**
```
Обращение → CategoryId = 10 (временно) → ИИ анализ → CategoryId = правильная категория
```

---

## 📖 Подробности

- `backend/CHANGELOG_CATEGORIES.md` - полное описание изменений
- `backend/InfoProject/Логика работы с категориями.txt` - техническая документация
- `backend/InfoProject/ИНСТРУКЦИЯ ПО ОБНОВЛЕНИЮ.txt` - детальные инструкции

---

**Готово! 🎉** Система теперь автоматически определяет правильные категории.

