# MVD Project - Полная документация проекта

**Версия:** 2.0.0  
**Дата:** Ноябрь 2025  
**Статус:** Готов к демонстрации

---

## 📋 Содержание

1. [Обзор проекта](#обзор-проекта)
2. [Архитектура системы](#архитектура-системы)
3. [Структура проекта](#структура-проекта)
4. [Backend (ASP.NET Core)](#backend-aspnet-core)
5. [Frontend (Flutter Web)](#frontend-flutter-web)
6. [База данных](#база-данных)
7. [API Документация](#api-документация)
8. [Установка и запуск](#установка-и-запуск)
9. [Разработка](#разработка)
10. [Тестирование](#тестирование)
11. [Развертывание](#развертывание)

---

## 🎯 Обзор проекта

### Описание

**MVD Project** — это система учёта обращений граждан в МВД России, разработанная в рамках производственной практики. Система позволяет гражданам подавать обращения о различных инцидентах, а сотрудникам МВД — обрабатывать их, анализировать с помощью AI и управлять рабочим процессом.

### Основные возможности

#### Для граждан (публичный доступ):
- ✅ Подача обращений без авторизации
- ✅ Проверка статуса обращения по номеру
- ✅ Автоматическое определение района по адресу
- ✅ Интерактивная карта инцидентов

#### Для сотрудников МВД:
- ✅ Авторизация по логину и паролю
- ✅ Дэшборд с KPI и интерактивной картой
- ✅ Список обращений с фильтрами и поиском
- ✅ Детальный просмотр обращений
- ✅ AI-анализ обращений (категория, приоритет, тональность)
- ✅ Управление статусами и назначение исполнителей
- ✅ Аналитика по районам и категориям
- ✅ Администрирование справочников (CRUD)
- ✅ Аудит изменений

### Технологический стек

**Backend:**
- ASP.NET Core 8.0
- Entity Framework Core 9.0
- PostgreSQL 16 + PostGIS
- Google Gemini API (AI)
- Nominatim (OSM) для геокодирования
- BCrypt для хеширования паролей

**Frontend:**
- Flutter 3.8+ (Web)
- Material 3
- go_router 14.2.0
- flutter_map 7.0.1
- syncfusion_flutter_charts 31.1.19
- Dio 5.7.0
- Hive 2.2.3 (кэширование)
- Riverpod 2.6.1 (state management)

---

## 🏗️ Архитектура системы

### Общая архитектура

```
┌─────────────────────────────────────────────────────────┐
│                    ПОЛЬЗОВАТЕЛИ                         │
├──────────────────┬──────────────────────────────────────┤
│   Граждане       │     Сотрудники МВД                   │
│   (публичный)    │     (авторизованные)                 │
└────────┬─────────┴──────────────┬──────────────────────┘
         │                        │
         └────────────┬───────────┘
                      │
         ┌────────────▼────────────┐
         │   FRONTEND (Flutter)    │
         │   - frontend-user       │
         │   - frontend-employee   │
         └────────────┬────────────┘
                      │ HTTP/REST
         ┌────────────▼────────────┐
         │   BACKEND (ASP.NET)     │
         │   - REST API            │
         │   - Services            │
         │   - Repositories        │
         └─────┬──────┬────────┬───┘
               │      │        │
       ┌───────┘      │        └────────┐
       │              │                 │
┌──────▼──────┐  ┌────▼────┐  ┌────────▼─────┐
│ PostgreSQL  │  │ Gemini  │  │  Nominatim   │
│ + PostGIS   │  │   API   │  │     OSM      │
│   (БД)      │  │  (AI)   │  │ (Геокодинг)  │
└─────────────┘  └─────────┘  └──────────────┘
```

### Архитектура Backend

```
┌────────────────────────────────────────┐
│         Presentation Layer             │
│  (Controllers, DTOs, Validators)       │
├────────────────────────────────────────┤
│          Business Logic Layer          │
│     (Services, Domain Logic)           │
├────────────────────────────────────────┤
│         Data Access Layer              │
│  (Repositories, EF Core Context)       │
├────────────────────────────────────────┤
│           Database Layer               │
│     (PostgreSQL + PostGIS)             │
└────────────────────────────────────────┘
```

### Архитектура Frontend

```
┌──────────────────────────────────────────┐
│              UI Layer                    │
│   (Screens, Widgets, Themes)             │
├──────────────────────────────────────────┤
│           Business Logic                 │
│   (State Management, Validators)         │
├──────────────────────────────────────────┤
│         Data Layer                       │
│  (API Service, Models, DTOs)             │
├──────────────────────────────────────────┤
│      Platform Layer                      │
│   (HTTP, Storage, Platform APIs)         │
└──────────────────────────────────────────┘
```

---

## 📁 Структура проекта

```
MVD_Project/
├── backend/                    # Backend API (ASP.NET Core 8)
│   ├── Controllers/            # REST API контроллеры
│   │   ├── AnalyticsController.cs
│   │   ├── AuthController.cs
│   │   ├── CategoriesController.cs
│   │   ├── CitizenRequestsController.cs
│   │   ├── CitizensController.cs
│   │   ├── DistrictsController.cs
│   │   ├── EmployeesController.cs
│   │   ├── NetworkController.cs
│   │   ├── RequestStatusesController.cs
│   │   └── RequestTypesController.cs
│   ├── Data/                   # База данных
│   │   ├── AppDbContext.cs     # DbContext
│   │   └── SeedData.cs         # Начальные данные
│   ├── DTOs/                   # Data Transfer Objects
│   ├── Models/                 # Модели данных
│   ├── Repositories/           # Репозитории (паттерн Repository)
│   ├── Services/               # Бизнес-логика
│   │   ├── GeminiService.cs    # AI интеграция
│   │   ├── NominatimService.cs # Геокодирование
│   │   └── AuditService.cs     # Аудит
│   ├── Migrations/             # Миграции EF Core
│   ├── Program.cs              # Точка входа
│   ├── appsettings.json        # Конфигурация
│   └── start.bat               # Скрипт запуска
│
├── frontend-user/              # Flutter проект для граждан
│   ├── lib/
│   │   ├── main.dart
│   │   ├── app_router.dart
│   │   ├── screens/
│   │   │   ├── citizen_complaint_screen.dart
│   │   │   └── check_status_screen.dart
│   │   ├── services/
│   │   │   ├── api_service.dart
│   │   │   └── cache_service.dart
│   │   ├── models/
│   │   └── utils/
│   ├── web/
│   └── pubspec.yaml
│
├── frontend-employee/          # Flutter проект для сотрудников
│   ├── lib/
│   │   ├── main.dart
│   │   ├── app_router.dart
│   │   ├── screens/
│   │   │   ├── auth_screen.dart
│   │   │   ├── dashboard_screen.dart
│   │   │   ├── request_list_screen.dart
│   │   │   ├── request_detail_screen.dart
│   │   │   ├── analytics_screen.dart
│   │   │   └── admin_screen.dart
│   │   ├── widgets/
│   │   ├── services/
│   │   ├── models/
│   │   └── utils/
│   ├── web/
│   └── pubspec.yaml
│
├── docs/                       # Документация
│   ├── ARCHITECTURE.md
│   └── DEVELOPMENT.md
│
├── start-user-new.bat         # Запуск версии для граждан
├── start-employee-new.bat     # Запуск версии для сотрудников
└── PROJECT_DOCUMENTATION.md   # Этот файл
```

---

## 🔧 Backend (ASP.NET Core)

### Обзор

Backend построен на ASP.NET Core 8.0 с использованием архитектуры Repository Pattern и разделения на слои (Controllers → Services → Repositories → Database).

### Основные компоненты

#### Controllers (REST API)

**1. AuthController** (`/api/auth`)
- `POST /api/auth/employee-login` — авторизация сотрудника
  - Принимает: `{ username, password }`
  - Возвращает: `{ id, username, role, employee: { id, fullName } }`

**2. CitizenRequestsController** (`/api/citizenrequests`)
- `GET /api/citizenrequests` — список обращений (с фильтрами)
- `GET /api/citizenrequests/{id}` — детали обращения
- `POST /api/citizenrequests` — создание обращения
- `PATCH /api/citizenrequests/{id}/status` — изменение статуса
- `PATCH /api/citizenrequests/{id}/assign` — назначение исполнителя
- `POST /api/citizenrequests/{id}/analyze` — AI-анализ обращения

**3. AnalyticsController** (`/api/analytics`)
- `GET /api/analytics/districts` — статистика по районам
- `GET /api/analytics/categories` — статистика по категориям
- `GET /api/analytics/ai` — статистика AI-анализа

**4. CategoriesController** (`/api/categories`)
- `GET /api/categories` — список категорий
- `GET /api/categories/{id}` — категория по ID
- `POST /api/categories` — создание категории
- `PUT /api/categories/{id}` — обновление категории
- `DELETE /api/categories/{id}` — удаление категории

**5. DistrictsController** (`/api/districts`)
- `GET /api/districts` — список районов
- `GET /api/districts/{id}` — район по ID
- `POST /api/districts` — создание района
- `PUT /api/districts/{id}` — обновление района
- `DELETE /api/districts/{id}` — удаление района

**6. EmployeesController** (`/api/employees`)
- `GET /api/employees` — список сотрудников
- `GET /api/employees/{id}` — сотрудник по ID
- `POST /api/employees` — создание сотрудника
- `PUT /api/employees/{id}` — обновление сотрудника
- `DELETE /api/employees/{id}` — удаление сотрудника

**7. RequestTypesController** (`/api/requesttypes`)
- `GET /api/requesttypes` — список типов обращений
- `POST /api/requesttypes` — создание типа
- `PUT /api/requesttypes/{id}` — обновление типа
- `DELETE /api/requesttypes/{id}` — удаление типа

**8. RequestStatusesController** (`/api/requeststatuses`)
- `GET /api/requeststatuses` — список статусов
- `POST /api/requeststatuses` — создание статуса
- `PUT /api/requeststatuses/{id}` — обновление статуса
- `DELETE /api/requeststatuses/{id}` — удаление статуса

**9. CitizensController** (`/api/citizens`)
- `GET /api/citizens` — список граждан
- `GET /api/citizens/{id}` — гражданин по ID
- `POST /api/citizens` — создание гражданина

**10. NetworkController** (`/api/network`)
- `GET /api/network/health` — проверка здоровья API

#### Services

**1. GeminiService** — интеграция с Google Gemini API
- Анализ текста обращений
- Определение категории, приоритета, тональности
- Генерация резюме и рекомендаций

**2. NominatimService** — геокодирование адресов
- Преобразование адреса в координаты (lat, lon)
- Использует OpenStreetMap Nominatim API

**3. AuditService** — логирование изменений
- Запись всех изменений в таблицу AuditLogs
- Отслеживание пользователя, времени и типа изменения

#### Repositories

Реализуют паттерн Repository для абстракции доступа к данным:

- `IRepository<T>` — базовый интерфейс
- `Repository<T>` — базовая реализация
- `ICitizenRequestRepository` — работа с обращениями
- `ICategoryRepository` — работа с категориями
- `IDistrictRepository` — работа с районами
- `IEmployeeRepository` — работа с сотрудниками
- `IUserRepository` — работа с пользователями
- И другие специализированные репозитории

#### Models (Сущности)

**1. CitizenRequest** — обращение гражданина
- Основные поля: `Id`, `RequestNumber`, `Description`, `IncidentLocation`, `CitizenLocation`
- Связи: `Citizen`, `Category`, `RequestType`, `RequestStatus`, `District`, `AcceptedBy`, `AssignedTo`
- AI поля: `AiCategory`, `AiPriority`, `AiSentiment`, `AiSummary`, `AiSuggestedAction`
- Геоданные: `Location` (geography point), `Latitude`, `Longitude`
- Временные метки: `CreatedAt`, `UpdatedAt`

**2. Citizen** — гражданин
- Поля: `Id`, `FirstName`, `LastName`, `Patronymic`, `Phone`, `Email`

**3. Employee** — сотрудник МВД
- Поля: `Id`, `FirstName`, `LastName`, `Patronymic`

**4. Category** — категория обращения
- Поля: `Id`, `Name`, `Description`

**5. District** — район
- Поля: `Id`, `Name`, `Description`

**6. RequestType** — тип обращения
- Поля: `Id`, `Name`, `Description`

**7. RequestStatus** — статус обращения
- Поля: `Id`, `Name`, `Description`

**8. User** — пользователь системы
- Поля: `Id`, `Username`, `PasswordHash`, `EmployeeId`, `RoleId`
- Связи: `Employee`, `Role`

**9. Role** — роль пользователя
- Поля: `Id`, `Name` (Admin, Manager, Operator)

**10. AuditLog** — лог изменений
- Поля: `Id`, `UserId`, `CitizenRequestId`, `Action`, `OldValue`, `NewValue`, `Timestamp`

### Конфигурация

**appsettings.json:**
```json
{
  "ConnectionStrings": {
    "DefaultConnection": "Host=localhost;Port=5432;Database=mvd_practice;Username=postgres;Password=postgres"
  },
  "Gemini": {
    "ApiKey": "YOUR_API_KEY"
  },
  "Kestrel": {
    "Endpoints": {
      "Http": {
        "Url": "http://0.0.0.0:5029"
      }
    }
  }
}
```

### CORS

Backend настроен на разрешение всех подключений (для разработки):
- `SetIsOriginAllowed(_ => true)`
- `AllowAnyMethod()`
- `AllowAnyHeader()`
- `AllowCredentials()`

### Миграции

Миграции EF Core находятся в `backend/Migrations/`. Для применения:
```bash
cd backend
dotnet ef database update
```

---

## 🎨 Frontend (Flutter Web)

### Обзор

Frontend разделён на два отдельных Flutter проекта:
1. **frontend-user** — публичный интерфейс для граждан
2. **frontend-employee** — интерфейс для сотрудников МВД

### Frontend-User (Граждане)

**Экраны:**
- `CitizenComplaintScreen` (`/complaint`) — подача обращения
- `CheckStatusScreen` (`/check-status`) — проверка статуса по номеру

**Зависимости:**
- Минимальный набор (без карт, аналитики)
- go_router, dio, shared_preferences, hive

**Порт:** 3000

### Frontend-Employee (Сотрудники)

**Экраны:**
- `AuthScreen` (`/`) — авторизация
- `DashboardScreen` (`/dashboard`) — главная панель с картой и KPI
- `RequestListScreen` (`/requests`) — список обращений с фильтрами
- `RequestDetailScreen` (`/requests/:id`) — детали обращения
- `AnalyticsScreen` (`/analytics`) — аналитика
- `AdminScreen` (`/admin`) — администрирование (только Admin/Manager)

**Зависимости:**
- Полный набор (карты, графики, аналитика)
- go_router, dio, flutter_map, syncfusion_flutter_charts, hive, riverpod

**Порт:** 4000

### Основные компоненты

#### Services

**ApiService** — централизованный HTTP клиент
- Использует Dio с interceptors
- Автоматическое логирование запросов/ответов
- Обработка ошибок
- Кэширование через Hive

**CacheService** — локальное кэширование
- Кэширование справочников (категории, статусы, районы)
- Использование Hive для быстрого доступа

#### Models

**CitizenRequestDto** — модель обращения
- Соответствует DTO из backend

**UserSession** — модель сессии пользователя
- Хранится в SharedPreferences

#### Navigation

**AppRouter** — конфигурация маршрутов
- Использует go_router
- Role-based access control
- Deep linking support

### State Management

- **Riverpod** — для управления состоянием
- **SharedPreferences** — для хранения сессии
- **Hive** — для кэширования данных

---

## 🗄️ База данных

### PostgreSQL + PostGIS

**Таблицы:**

1. **Users** — пользователи системы
2. **Roles** — роли (Admin, Manager, Operator)
3. **Employees** — сотрудники МВД
4. **Citizens** — граждане
5. **CitizenRequests** — обращения граждан
6. **Categories** — категории обращений
7. **RequestTypes** — типы обращений
8. **RequestStatuses** — статусы обращений
9. **Districts** — районы
10. **AuditLogs** — логи изменений

### Геоданные

- Тип `geography(point)` для хранения координат
- PostGIS для геопространственных запросов
- Автоматическое определение координат через Nominatim

### Связи

- `CitizenRequest` → `Citizen` (многие к одному)
- `CitizenRequest` → `Category` (многие к одному)
- `CitizenRequest` → `District` (многие к одному)
- `CitizenRequest` → `Employee` (AcceptedBy, AssignedTo)
- `User` → `Employee` (один к одному)
- `User` → `Role` (многие к одному)

---

## 📡 API Документация

### Базовый URL

```
http://localhost:5029/api
```

### Swagger UI

В режиме разработки доступен Swagger UI:
```
http://localhost:5029
```

### Основные эндпоинты

#### Авторизация

**POST /api/auth/employee-login**
```json
Request:
{
  "username": "kozlova",
  "password": "operator123"
}

Response:
{
  "id": 1,
  "username": "kozlova",
  "role": "Operator",
  "employee": {
    "id": 1,
    "fullName": "Козлов Александр Сергеевич"
  }
}
```

#### Обращения

**GET /api/citizenrequests**
- Параметры: `categoryId`, `districtId`, `statusId`
- Возвращает: список обращений

**POST /api/citizenrequests**
```json
Request:
{
  "citizen": {
    "firstName": "Иван",
    "lastName": "Иванов",
    "patronymic": "Иванович",
    "phone": "+79001234567",
    "email": "ivan@example.com"
  },
  "description": "Описание инцидента",
  "incidentLocation": "ул. Ленина, 1",
  "citizenLocation": "ул. Ленина, 1",
  "categoryId": 1,
  "requestTypeId": 1
}
```

**PATCH /api/citizenrequests/{id}/status**
```json
Request:
{
  "statusId": 2
}
```

**PATCH /api/citizenrequests/{id}/assign**
```json
Request:
{
  "employeeId": 1
}
```

#### Аналитика

**GET /api/analytics/districts**
- Возвращает статистику по районам

**GET /api/analytics/categories**
- Возвращает статистику по категориям

**GET /api/analytics/ai**
- Возвращает статистику AI-анализа

#### Справочники

**GET /api/categories** — список категорий
**GET /api/districts** — список районов
**GET /api/requesttypes** — список типов
**GET /api/requeststatuses** — список статусов
**GET /api/employees** — список сотрудников

---

## 🚀 Установка и запуск

### Предварительные требования

- **Windows 10/11** (или Linux/macOS)
- **.NET SDK 8.0+**
- **PostgreSQL 16** с PostGIS
- **Flutter SDK 3.8+**
- **Git**

### Установка

#### 1. Клонирование репозитория
```bash
git clone <URL>
cd MVD_Project
```

#### 2. Настройка базы данных

Создайте базу данных PostgreSQL:
```sql
CREATE DATABASE mvd_practice;
CREATE EXTENSION postgis;
```

Настройте строку подключения в `backend/appsettings.json`:
```json
{
  "ConnectionStrings": {
    "DefaultConnection": "Host=localhost;Port=5432;Database=mvd_practice;Username=postgres;Password=postgres"
  }
}
```

#### 3. Применение миграций
```bash
cd backend
dotnet ef database update
```

#### 4. Настройка API ключей

В `backend/appsettings.json` укажите API ключ Google Gemini:
```json
{
  "Gemini": {
    "ApiKey": "YOUR_GEMINI_API_KEY"
  }
}
```

#### 5. Установка зависимостей Frontend

**Для версии пользователей:**
```bash
cd frontend-user
flutter pub get
```

**Для версии сотрудников:**
```bash
cd frontend-employee
flutter pub get
```

### Запуск

#### Вариант 1: Автоматический запуск (Windows)

**Для пользователей:**
```cmd
start-user-new.bat
```

**Для сотрудников:**
```cmd
start-employee-new.bat
```

#### Вариант 2: Ручной запуск

**Терминал 1 — Backend:**
```bash
cd backend
dotnet run
# или для hot reload:
dotnet watch run
```

**Терминал 2 — Frontend-User:**
```bash
cd frontend-user
flutter run -d chrome --web-port=3000
```

**Терминал 3 — Frontend-Employee:**
```bash
cd frontend-employee
flutter run -d chrome --web-port=4000
```

### Доступ к приложению

- **Backend API:** http://localhost:5029
- **Swagger UI:** http://localhost:5029
- **Frontend-User:** http://localhost:3000/complaint
- **Frontend-Employee:** http://localhost:4000/

### Тестовые данные

**Авторизация сотрудника:**
- Логин: `kozlova`
- Пароль: `operator123`
- Роль: Operator

**Другие пользователи:**
- `nikitina` / `manager123` (Manager)
- `fedorov` / `admin123` (Admin)

---

## 💻 Разработка

### Структура веток Git

- `main` — стабильная версия
- `develop` — разработка
- `feature/*` — новые функции
- `bugfix/*` — исправления

### Code Style

**C#:**
- Следуем Microsoft C# Coding Conventions
- Используем nullable reference types
- Async/await для всех асинхронных операций

**Dart/Flutter:**
- Следуем Effective Dart Style Guide
- Используем const конструкторы где возможно
- Разделение на мелкие виджеты

### Добавление нового эндпоинта (Backend)

1. **Создать DTO** (если нужно):
```csharp
// DTOs/MyDto.cs
public class MyDto
{
    public string Name { get; set; }
}
```

2. **Добавить метод в Controller:**
```csharp
[HttpPost]
public async Task<IActionResult> Create([FromBody] MyDto dto)
{
    // Логика
    return Ok(result);
}
```

3. **Добавить метод в Repository** (если нужно):
```csharp
public async Task<MyEntity> GetByNameAsync(string name)
{
    return await _context.MyEntities
        .FirstOrDefaultAsync(e => e.Name == name);
}
```

### Добавление нового экрана (Frontend)

1. **Создать экран:**
```dart
// lib/screens/my_screen.dart
class MyScreen extends StatefulWidget {
  const MyScreen({super.key});
  
  @override
  State<MyScreen> createState() => _MyScreenState();
}
```

2. **Добавить маршрут в AppRouter:**
```dart
GoRoute(
  path: '/my-screen',
  builder: (context, state) => const MyScreen(),
),
```

3. **Добавить метод в ApiService** (если нужно):
```dart
Future<MyData> getMyData() async {
  final response = await _dio.get('/api/mydata');
  return MyData.fromJson(response.data);
}
```

---

## 🧪 Тестирование

### Backend

```bash
cd backend
dotnet test
```

### Frontend

```bash
cd frontend-user
flutter test

cd frontend-employee
flutter test
```

---

## 📦 Развертывание

### Backend

1. **Сборка:**
```bash
cd backend
dotnet publish -c Release -o ./publish
```

2. **Запуск:**
```bash
cd publish
./MvdBackend
```

### Frontend

1. **Сборка:**
```bash
cd frontend-user
flutter build web --release

cd frontend-employee
flutter build web --release
```

2. **Размещение:**
- Разместите содержимое `build/web/` на веб-сервере (nginx, Apache, IIS)
- Настройте CORS на backend для домена frontend

### Docker (планируется)

```bash
docker-compose up
```

---

## 🔐 Безопасность

### Текущая реализация

- BCrypt для хеширования паролей
- CORS настроен для разработки
- Валидация входных данных
- Аудит изменений

### Рекомендации для продакшена

- JWT токены для авторизации
- HTTPS обязателен
- Rate limiting
- Валидация и санитизация всех входных данных
- Логирование безопасности
- Регулярные обновления зависимостей

---

## 📊 Производительность

### Оптимизации

- EF Core: Include для eager loading
- API: Pagination для больших списков
- Frontend: Виртуализация списков
- Кэширование справочников через Hive
- Ленивая загрузка компонентов

### Мониторинг

- Логирование через ILogger
- Health check endpoint: `/api/network/health`
- Swagger UI для тестирования API

---

## 🐛 Известные проблемы и ограничения

1. **Авторизация:** Временное хранение сессии в SharedPreferences (планируется JWT)
2. **AI анализ:** Синхронный вызов API (планируется асинхронная обработка)
3. **Геокодирование:** Зависимость от внешнего сервиса Nominatim
4. **Масштабирование:** Однопоточный backend (планируется load balancing)

---

## 📈 Планы развития

- [ ] JWT авторизация
- [ ] Docker контейнеризация
- [ ] CI/CD pipeline
- [ ] Мобильные приложения (iOS/Android)
- [ ] Пуш-уведомления
- [ ] Экспорт отчетов (PDF, Excel)
- [ ] Интеграция с внешними системами
- [ ] Многоязычность (i18n)

---

## 📞 Контакты и поддержка

Проект разработан в рамках производственной практики МВД России.

По вопросам обращайтесь к руководителю практики.

---

## 📄 Лицензия

Учебный проект. Все права защищены.

---

**Версия документации:** 2.0.0  
**Последнее обновление:** Ноябрь 2025  
**Автор:** MVD Practice Team

